<?php

namespace App\Http\Controllers;


use App\Models\Appointment;
use App\Models\RaftCompanyBox;

class DevelopmentController extends Controller
{
    public function dev()
    {
        //[$boxes, $camps] = Appointment::getCampsOfCreate();
        //$f = RaftCompanyBox::query()->where('seen_notes', !0)->select('box')->groupBy('box')->get()->toArray();
        //d($boxes);
        //d($boxes,$f);
        //d($boxes, $camps);
    }
}
